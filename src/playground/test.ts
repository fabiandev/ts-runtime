const f = () => {

  let x: ComplexType = () => {
    const a: boolean = true;
    return a;
  };

  const b: number = 10;
  let c: string;
  let d: boolean[] = [true, false]

  const e: Array<Array<Array<number>>> = [false];

  c = 'hi';

  let f: [one, two];
};
